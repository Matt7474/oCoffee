-- Generated by Mocodo 4.2.12

CREATE TABLE CARACTERISTIC (
  PRIMARY KEY (id),
  id    VARCHAR(42) NOT NULL,
  carac VARCHAR(42)
);

CREATE TABLE COFFEE (
  PRIMARY KEY (id),
  id               VARCHAR(42) NOT NULL,
  name             VARCHAR(42),
  reference        VARCHAR(42),
  description      VARCHAR(42),
  price_kilo       VARCHAR(42),
  origin_id        VARCHAR(42),
  caracteristic_id VARCHAR(42),
  disponibility_id VARCHAR(42),
  created_at       VARCHAR(42),
  updated_at       VARCHAR(42),
  id_2             VARCHAR(42) NULL,
  id_3             VARCHAR(42) NULL
);

CREATE TABLE COMPTER (
  PRIMARY KEY (id_1, id_2),
  id_1 VARCHAR(42) NOT NULL,
  id_2 VARCHAR(42) NOT NULL
);

CREATE TABLE DISPONIBILITY (
  PRIMARY KEY (id),
  id    VARCHAR(42) NOT NULL,
  dispo VARCHAR(42)
);

CREATE TABLE ORIGIN (
  PRIMARY KEY (id),
  id   VARCHAR(42) NOT NULL,
  name VARCHAR(42)
);

ALTER TABLE COFFEE ADD FOREIGN KEY (id_3) REFERENCES CARACTERISTIC (id);
ALTER TABLE COFFEE ADD FOREIGN KEY (id_2) REFERENCES DISPONIBILITY (id);

ALTER TABLE COMPTER ADD FOREIGN KEY (id_2) REFERENCES COFFEE (id);
ALTER TABLE COMPTER ADD FOREIGN KEY (id_1) REFERENCES ORIGIN (id);
